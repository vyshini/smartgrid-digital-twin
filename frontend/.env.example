# This frontend has no environment variables of its own to configure.
# It talks to your real FastAPI backend via the dev-server proxy defined
# in vite.config.ts ('/api' -> http://localhost:8000). If you deploy the
# built frontend separately from the backend, update that proxy target
# (or serve behind a reverse proxy that forwards /api to your backend).
